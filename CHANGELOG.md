# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の書式に従う。バージョンは `VERSION` ファイルが正で、`install.sh` が導入先に `.claude/skills/.hotl-version` として記録する。

1.0.0 の条件は「サンプルプロジェクトでの実地 end-to-end 実行が完走すること」。それまでは 0.x（レビューによる検証のみ）。

## [0.9.0] - 2026-09-05

初めてバージョンを付けた版。2026-08-28 の初版から 42 周の敵対的レビューを経た到達点（各周は `plans/review-log.md`）。

### 追加

- **ワークフロー基盤**: ヒアリング → 要件定義 → 承認ゲート（唯一のブロッキング）→ 仕様化 → 設計 → 開発 → done のステートマシン（`skills/hotl/`）
- **AgentTrail**（`docs/log.md`）: 追記専用の判断証跡。type は report / decision / finding / approval / interrupt / proposal
- **再開性**: state.json（phase・sha256 承認アンカー）＋ trail ＋ tasks.md チェックボックスから、どの時点で落ちても続きから再開
- **安全装置 B-1〜B-6**: 独立コードレビュー（`checklist-code.md`）／決定論的な検証ツールチェーン／指摘の3分類・BLOCKED 報告書式・証拠ルール／進行中表示 `[>]` と完了時刻／done 前の整合性監査／読み込み規律
- **非機能の横断チェック**: 要件テンプレの5観点（性能・費用・セキュリティ前提・データ保全・運用）と、受け入れ検証での攻撃面・性能実測・費用実測（`checklist-security.md`）
- **UX ウォークスルー**: done 前に、コードを読まず利用者の視点で成果物を評価する検証（P6）
- **コンセプトレベルの提案**（P2）: ヒアリングでアプリの形を2〜3案提示し、採否は人間が決める
- **hotl-pm**: 複数プロジェクトを横断する旗振りスキル（`install.sh --pm`）
- **レビュー台帳** `plans/review-log.md`: 変更プロセスの1周＝1行。「残作業ゼロ」の判定条件を明文化
- **機械的整合チェック** `scripts/check-consistency.sh`: ファイル間の相互参照・phase 名・state フィールド等の一致を決定論的に検査（AI レビュアーの目視から除外）
- **VERSION / CHANGELOG**: 導入先から版を特定できるようにした
- **選択で答えられることは選択式で聞く**（共通規則）: **返信を待ってターンを終了する場面に限り**（承認ゲート・再確認・⚠️ キャップ超過の A/B/C・環境の確認）AskUserQuestion で提示する（自由記述が要る選択肢は「その他」で書ける旨を添える）。BLOCKED・提案・中間報告など**返信を待たない報告は選択式にしない**（自律ループを人間待ちで止めないため）。選択式が使えない環境では同じ選択肢をテキストで出す

### 修正

- **人間の入力の経路の食い違い**: `hearing` 中に inbox 経由で届いた入力の扱いが SKILL.md Step 4（未確認事項への回答・ゲート応答のみ免除）と Step 7（停止・状況確認を除くすべて免除）で異なり、ヒアリング中の新しい要望が承認リセット経路に流れ得た。Step 7 に合わせて一致させた
- **対応記録の語の食い違い**: 修正依頼の対応記録が SKILL.md「回答として処理」と 02「修正済み」で不一致だった。語彙の正を reporting.md に一本化。inbox 経由の承認にも対応記録が付くようにした（整合性監査が「未処理 interrupt」と誤検出しうる状態を解消）
- **hotl-pm の cannot-continue の外延**: 定義（4種）と受領規則（2種）でずれていた点を明示し、Step 3 と Step 4 の二重記述を一本化した
- 到達しない規則の解消（「チェックリスト適用のみのレビューは低 effort 可」は呼び出し側が effort に触れないため削除し、レビュー・検証・監査役の非ダウングレード規則に effort を統合）
- **承認の時点検査**: 現在のゲート提示より前に届いた「承認します」が、次の起動で現在の版への承認として消費され得た（人間が読んでいない版が承認扱いになる）。直近の `requirements -> awaiting_approval` 遷移より後の発言のみを承認とする規則を追加
- **タスク追加の挿入位置**: 方針変更で追加したタスクを末尾に足すと「検証タスクは常に末尾」の不変条件が破れ、検証を受けないタスクのまま done に到達し得た。挿入位置を②と同じ（検証タスク直前）に固定し、done の前提に「`[-]` を除く最後の行が検証タスクの `[x]`」を追加
- クラッシュ窓の修復を追加（inbox 転記後・削除前の二重転記／`[x]` 後・commit 前の取りこぼし／Step 6 の state 更新と trail 追記の間で遷移報告が欠落）、`repo: "parent"` でコミット作者設定の確認までスキップしていた点、委譲実行で cwd が戻る環境の `git log` 誤判定、ワークスペースで hotl と hotl-pm のどちらが起動するかで実行形態が変わっていた点、進行中タスク全体が BLOCKED になったときのチェックボックス状態、`.hotl-version` が上書き拒否後も更新されていた点 ほか MINOR 18・NIT 6

### 修正（実地ドライランで判明）

- **全称の受け入れ基準が done を到達不能にする**: 「すべてのエラーメッセージが日本語」のような基準は、検証者が毎周あたらしい失敗経路（文字コード・パイプ切断・書き込み失敗）を作れるため、修正しても合格を示せない。要件チェックリストに「全称・全数の基準は検査対象を有限に列挙できるか」を追加し、02 の既定値の書き方を適用範囲つきに、05 は「同じ基準が3周とも別経路で不合格なら基準の書き方の問題として B（緩和）を推奨する」に
- **UX ウォークスルーの「必ず直すべき」がスコープ外だと分類の隙間に落ちる**: ①でも②でも扱えず proposal に落ちて未修正のまま done に到達し得た。スコープ外は要件レベルの変更として承認リセットでゲートに戻す経路を明記し、最終報告では「使ってみて困る点（未対応）」を改善提案と別枠にした
- **`git add -N .` への誘導**: 新規ファイルのタスクで diff を作ろうとするとインデックスが汚れる（実際に `.claude/skills/**` 16ファイルが intent-to-add で並んだ）。`git diff --no-index /dev/null <file>` を明記し、`git add -N .` を禁止に追加
- **作業中の「いまどうなってる？」で自律区間が止まり得た**: 割り込み分類に「0. 状況確認 → 1〜2行で答えて続行」を追加し、ターン終了できる状況確認を「起動メッセージが状況確認だった場合」に限定
- 検証が失敗したまま commit できた点／完了証拠に実行前の見込み値を書けた点／要件の漏れチェックが一方向だった点（作成者が独断で足した要件を三重に見逃す）／セキュリティ MAJOR の修正後に再確認が無かった点／検証フェーズの実装変更が spec・design に追随しない点／監査2周目の修正が未検査のまま完了する点／コードレビューが設定ファイルで発散する点 ほか

### 修正（実行環境）

- **`scripts/check-consistency.sh` が macOS の標準ターミナルで異常終了していた**（`LANG=ja_JP.UTF-8` × bash 3.2 の組み合わせ。`$var` の直後に多バイト文字が続くコードを bash 3.2 のパーサが誤解析する）。C ロケールで動く環境（Claude Code の Bash など）では通っていたため気づきにくかった。スクリプト内で `LC_ALL=C` に固定し、UTF-8 ロケール × bash 3.2・zsh 経由・C ロケールの3通りで同一結果を確認
- `install.sh` の対話モードで上書きを拒否すると、同じ原因で `unbound variable` 終了していた（`--pm` だけを足す手順がヘッダに書かれているのに実行できなかった）

### 追加（レビューの収束）

- **`adr/`（Architecture Decision Record）**: 振る舞いを決めた判断を1件1ファイルで記録する（決定・理由・**退けた案**・**再検討の条件**・再指摘の履歴）。ADR-001〜010 を起票。**レビュアーには渡さず**（敵対性を保つため）、指摘を受けた側が照合して「supersede するか却下するか」を判断する
- **`plans/regression-scenarios.md`**: 過去の CRITICAL/MAJOR を40シナリオに落としたもの。毎周のレビューで全件通してから新規探索する
- **`plans/review-backlog.md`**: PASS 周の MINOR/NIT を積む場所（反映して再周する運用をやめた）
- `scripts/check-consistency.sh` に ADR 索引の整合検査を追加（319 検査）
- **`plans/findings-ledger.md`（指摘台帳）**: 1指摘1行で「適用した対応」と「その後（その対応が生んだ指摘）」を追跡。修正前に領域タグで引き、過去に欠陥を生んだ対応の繰り返しを禁止
- **変更プロセスの有界化（ADR-011）**: 敵対的レビューは最大2周、合格条件は「回帰 NG ゼロ かつ CRITICAL ゼロ」。2周目の MAJOR は直さず次の変更の入力に。構造に触る変更は実地ドライラン必須

### 変更（2026-09-06 — バックログ筆頭）

- **差分更新時の要件レビューは変更部分に限定**（ADR-013）: 承認済み本文への指摘が周ごとに出続け、ゲートに「17件未反映」が付く問題を解消
- **PM の done 行**: 「新要望があれば」の判定を PM から外し、常にメインループで hotl を実行（承認リセット途中死の後の「続きから」が固定されていた）
- **diff・コード・ログはパス渡し**: インラインで渡すとエスケープが変換され、実ファイルに無い欠陥が指摘された

### 検証（2026-09-06）

- **実地ドライラン2回目**（在庫メモ CLI）: 要件レビュー途中で落ちたセッションからの「続きから」再開が規定どおり効き、**done に到達**（2時間39分）。所見 MAJOR 9 / MINOR 24 / NIT 7 は `plans/dryrun/2026-09-06-stock-memo.md` と `plans/2026-09-lean-condensation.md` 付録2 に記録（次の変更の入力）

### 追加（2026-09-06 — 途中で止まっても指示を取りこぼさず再開する）

- **共通手順「状態更新」（ADR-012）**: 承認・承認リセット・フェーズ遷移のように複数ファイルを書き換える更新は `更新開始/更新完了` で囲み、各書き込みを冪等にする。再開時は Step 4 冒頭で「完了の無い開始」を種別ごとにやり直す。場面ごとに散っていた4つのクラッシュ復旧規則を削除
- **1 interrupt = 1 指示**: 「承認します。あと CSV が壊れてる」のような複数指示は記録段階で分割し、それぞれに対応記録を持たせる
- **検証の独立性**: PM の検証チャンクはコードを変更しない（inbox 由来の実装タスクは中断で戻す）。サブエージェント不可でインライン代替した検証は「独立検証なし」を trail に残し最終報告で開示

### 変更

- 承認済み要件の凍結（`approval.approved` が true の間は requirements.md を書き換えない）を SKILL.md の共通規則に格上げ
- 承認リセットを3点セット（state 初期化・承認記録節の巻き戻し・phase 戻し）の共通手順に一本化

### 削除

- **nanoclaw / マルチランタイム対応**（2026-08-29）。ランタイムは Claude Code のみ。再導入しない（PRINCIPLES アンチスコープ）
