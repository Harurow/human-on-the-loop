# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の書式に従う。バージョンは `VERSION` ファイルが正で、`install.sh` が導入先に `.claude/skills/.hotl-version` として記録する。

1.0.0 の条件は「サンプルプロジェクトでの実地 end-to-end 実行が完走すること」。それまでは 0.x（レビューによる検証のみ）。

## [0.9.0] - 2026-09-05

初めてバージョンを付けた版。2026-08-28 の初版から 42 周の敵対的レビューを経た到達点（各周は `plans/review-log.md`）。

### 追加

- **ワークフロー基盤**: ヒアリング → 要件定義 → 承認ゲート（唯一のブロッキング）→ 仕様化 → 設計 → 開発 → done のステートマシン（`skills/hotl/`）
- **AgentTrail**（`docs/log.md`）: 追記専用の判断証跡。type は report / decision / finding / approval / interrupt / proposal
- **再開性**: state.json（phase・sha256 承認アンカー）＋ trail ＋ tasks.md チェックボックスから、どの時点で落ちても続きから再開
- **安全装置 B-1〜B-6**: 独立コードレビュー（`checklist-code.md`）／決定論的な検証ツールチェーン／指摘の3分類・BLOCKED 報告書式・証拠ルール／進行中表示 `[>]` と完了時刻／done 前の整合性監査／読み込み規律
- **非機能の横断チェック**: 要件テンプレの5観点（性能・費用・セキュリティ前提・データ保全・運用）と、受け入れ検証での攻撃面・性能実測・費用実測（`checklist-security.md`）
- **UX ウォークスルー**: done 前に、コードを読まず利用者の視点で成果物を評価する検証（P6）
- **コンセプトレベルの提案**（P2）: ヒアリングでアプリの形を2〜3案提示し、採否は人間が決める
- **hotl-pm**: 複数プロジェクトを横断する旗振りスキル（`install.sh --pm`）
- **レビュー台帳** `plans/review-log.md`: 変更プロセスの1周＝1行。「残作業ゼロ」の判定条件を明文化
- **機械的整合チェック** `scripts/check-consistency.sh`: ファイル間の相互参照・phase 名・state フィールド等の一致を決定論的に検査（AI レビュアーの目視から除外）
- **VERSION / CHANGELOG**: 導入先から版を特定できるようにした
- **選択で答えられることは選択式で聞く**（共通規則）: **返信を待ってターンを終了する場面に限り**（承認ゲート・再確認・⚠️ キャップ超過の A/B/C・環境の確認）AskUserQuestion で提示する（自由記述が要る選択肢は「その他」で書ける旨を添える）。BLOCKED・提案・中間報告など**返信を待たない報告は選択式にしない**（自律ループを人間待ちで止めないため）。選択式が使えない環境では同じ選択肢をテキストで出す

### 修正

- **人間の入力の経路の食い違い**: `hearing` 中に inbox 経由で届いた入力の扱いが SKILL.md Step 4（未確認事項への回答・ゲート応答のみ免除）と Step 7（停止・状況確認を除くすべて免除）で異なり、ヒアリング中の新しい要望が承認リセット経路に流れ得た。Step 7 に合わせて一致させた
- **対応記録の語の食い違い**: 修正依頼の対応記録が SKILL.md「回答として処理」と 02「修正済み」で不一致だった。語彙の正を reporting.md に一本化。inbox 経由の承認にも対応記録が付くようにした（整合性監査が「未処理 interrupt」と誤検出しうる状態を解消）
- **hotl-pm の cannot-continue の外延**: 定義（4種）と受領規則（2種）でずれていた点を明示し、Step 3 と Step 4 の二重記述を一本化した
- 到達しない規則の解消（「チェックリスト適用のみのレビューは低 effort 可」は呼び出し側が effort に触れないため削除し、レビュー・検証・監査役の非ダウングレード規則に effort を統合）
- **承認の時点検査**: 現在のゲート提示より前に届いた「承認します」が、次の起動で現在の版への承認として消費され得た（人間が読んでいない版が承認扱いになる）。直近の `requirements -> awaiting_approval` 遷移より後の発言のみを承認とする規則を追加
- **タスク追加の挿入位置**: 方針変更で追加したタスクを末尾に足すと「検証タスクは常に末尾」の不変条件が破れ、検証を受けないタスクのまま done に到達し得た。挿入位置を②と同じ（検証タスク直前）に固定し、done の前提に「`[-]` を除く最後の行が検証タスクの `[x]`」を追加
- クラッシュ窓の修復を追加（inbox 転記後・削除前の二重転記／`[x]` 後・commit 前の取りこぼし／Step 6 の state 更新と trail 追記の間で遷移報告が欠落）、`repo: "parent"` でコミット作者設定の確認までスキップしていた点、委譲実行で cwd が戻る環境の `git log` 誤判定、ワークスペースで hotl と hotl-pm のどちらが起動するかで実行形態が変わっていた点、進行中タスク全体が BLOCKED になったときのチェックボックス状態、`.hotl-version` が上書き拒否後も更新されていた点 ほか MINOR 18・NIT 6

### 変更

- 承認済み要件の凍結（`approval.approved` が true の間は requirements.md を書き換えない）を SKILL.md の共通規則に格上げ
- 承認リセットを3点セット（state 初期化・承認記録節の巻き戻し・phase 戻し）の共通手順に一本化

### 削除

- **nanoclaw / マルチランタイム対応**（2026-08-29）。ランタイムは Claude Code のみ。再導入しない（PRINCIPLES アンチスコープ）
