# 設計原則（PRINCIPLES）

このドキュメントは human-on-the-loop の思想と、フレームワーク自体を変更するときの作法を定める。
**コンテキスト（セッション・担当エージェント）が切り替わっても同じ品質を保つための基準**であり、フレームワークに手を入れる者（人間・AI とも）は必ず最初に読む。

## P1: Human-on-the-loop

人間は「ループの中」ではなく「上」にいる。人間をブロックするのは**要件承認の一度だけ**。それ以外の報告はすべて非ブロッキング（返信を待たない）。人間の割り込みはいつでも受け付けるが、承認済み要件を無効化する変更（要件レベルの変更）だけはゲートに戻す。

## P2: 要求と真のニーズを区別し、ニーズを超える提案をする

要件はユーザーの要求から作るが、**要求と「真に欲しいもの」は往々にして異なる**。ヒアリングは機能の聞き取りではなく課題の発掘である（5-whys・具体シナリオの再現・非ゴールの確認）。

さらにエージェントは**コンサルタント**として、コア課題の理解に基づき**ユーザーが言語化していない改善を能動的に提案する責務**を持つ。提案は**コンセプトレベル**（「こんな形のアプリはどうですか」— 課題に対する別の形・より良い形を2〜3案。**期待の一段・二段上をいく案を必ず含める**）から**機能レベル**（隣接する困りごと・自動化・UX）まで。ただし:

- 提案は「提案」と明示し、**採否は必ず人間が決める**。黙って作る（スコープクリープ）のとは別物
- 提案の経路は3つ: ヒアリング中の提案（コンセプト・機能。採用されれば MVP スコープ／出所「提案」の要件になる）、開発中に見つけた改善（trail に `proposal` 記録 → 最終報告で提示）、次イテレーションの候補

## P3: AgentTrail — すべての判断に証跡を残す

エージェントの判断・行動・根拠は `docs/log.md`（**AgentTrail**）に**追記専用**で記録する。目的は2つ:

1. コンテキストが切り替わっても、新しいエージェントが state + trail だけで同じ品質で再開できる
2. 人間が後から「なぜそうなったか」を監査できる

書式と type 分類（report / decision / finding / approval / interrupt / proposal）は `skills/hotl/playbooks/reporting.md` を正とする。過去エントリの書き換えは禁止。フェーズ遷移の commit に含めることで git 履歴が改ざん耐性を与える。

## P4: コンテキスト分離

フェーズ間の受け渡しは docs/ の成果物ドキュメントのみ。会話履歴を下流に持ち込まない。**作成と検証は必ず別コンテキスト**（要件レビュアー・受け入れ検証者は fresh context のサブエージェント）。実装ループだけはコード知識の連続性を優先して分割しない。

## P5: 再開性 — いつ死んでも続きから

どの時点でセッションが切れても、state + trail + 成果物から正確に再開できる状態を保つ。具体的には:

- 破壊的再生成の禁止: 既存成果物は常に差分更新。テンプレートからの作り直しは初回のみ
- 進捗台帳（tasks.md のチェックボックス）の保護と、未コミット WIP の明示的処置（引き継ぐ/破棄を判断して trail に記録）
- 人間の入力（修正依頼等）は、処理を始める前にまず trail に記録する（クラッシュで失わない）

## P6: 信頼せず検証する

- 承認は requirements.md の sha256 アンカー付きで記録し、再開時に照合する。不一致・不変条件違反（自律区間なのに未承認）は自律続行を拒否してゲートへ戻す
- 成果物はトレーサビリティ（要件 R-n → 仕様 S-n → 設計モジュール → タスク T-n）で対応漏れゼロを機械的に確認する
- 完成物は作成者ではなく fresh context の検証者が受け入れ基準と照合する
- 受け入れ基準の合格は機能の正しさしか保証しない。完成物は**作ったシステムを利用する人の視点**でも検証する（05 の UX ウォークスルー: コードを読まず、利用者として実際の画面・出力を使って評価し、利用者の言葉で指摘する）

## P7: Lean — 文書はプロジェクト規模に比例

厚い文書自体に価値はない。小規模プロジェクトなら各ドキュメントは簡潔でよい。フレームワーク自体も、実際に効くファイル・ルールだけを置く。

## P8: 自律的改善 — 計測して選び、劣る方法は理由付きで封印する

作業の進め方そのものを改善対象とする。方法の優劣が結果に効きそうな場面では、小さく試して客観指標（速度・成功率・単純さ）で前後比較し、良い方を採択する。比較結果・退けた理由・**再試行条件**は知見台帳（`docs/lessons.md`）に記録し、**再試行条件が満たされない限り劣る方法を使わない**。同じ方法で2回失敗したら方法自体を疑う。計測のコストが期待される節約を超える場合は計測しない（P7 に従う）。

フレームワーク自体の開発にも同じ規律を適用する: レビューの反復で得た知見は、その場限りにせず PRINCIPLES・playbook に構造として反映する。

## アンチスコープ — 導入しないと決めたもの

検討の結果、意図的に導入しないもの。再検討するときは、ここに書かれた理由が無効になったことを確認してから行う:

- **タスク単位の重い実行契約（10項目超の必須記帳）**: タスク粒度は半日以下であり、記帳がタスク本体より重くなる（P7）。目的・入出力・開始/完了条件はフェーズ（playbook）レベルで定義済み。タスクには「検証可能な完了条件」と証拠ルールだけを課す
- **進捗集計の永続ファイル化・状態ファイルの複製**: 進捗の正本は tasks.md のチェックボックス1つ（D6）。ヘッダー集計・別ファイルの状態複製は正本の重複であり、必ず食い違う（hotl-pm の状況一覧のような**都度導出の表示**はこれに該当しない — ファイルに書かない限り正本と食い違えない）
- **遠隔通知機構**: 通知の送り手が現存しない。境界原則のみ保持する — **正本となる指示は常にファイル（docs/）に保存し、通知には本文を載せず「正本を読み直す契機」だけを渡す。通知が失敗・不在でもワークフローは成立する**
- **常設のレビュー用エージェント定義**: fresh context のサブエージェント呼び出し（playbook が入力を指定）で同じ分離が得られる。定義ファイルは配布単位を複雑にする
- **高頻度のリポジトリ横断監査**: フェーズ遷移ごとの双方向検算が同じ欠陥クラスを検査済み。横断監査は done 前の節目のみ（修正後の再突入時は前回指摘を渡した上で新規指摘に限定、最大2周 — 05 参照）
- **nanoclaw / マルチランタイム対応**: 2026-08 に撤去済み。再導入しない

## フレームワーク自体の変更プロセス

フレームワーク（`skills/` 配下の全スキル — hotl・hotl-pm — の SKILL.md / playbooks / templates、および install.sh）に変更を加えたら、次を回す。**周回は有界**で、合格条件は「回帰 NG ゼロ かつ CRITICAL ゼロ」。MAJOR ゼロは条件にしない（規則の相互作用を fresh context がサンプリングする以上、MAJOR ゼロは到達不能な基準で、追いかけると修正が次の欠陥を生み続ける — 2026-09-05 に10周で実証済み）。

1. **実装して commit する**。振る舞いを決める判断をしたら `adr/` に ADR を1枚書く（決定・理由・退けた案・再検討の条件）
2. `scripts/check-consistency.sh` で **FAIL ゼロ**を確認する（相互参照・phase 名・state フィールド・ADR 索引・対応記録の語彙は機械で検査する。レビュアーの目をここに使わない）
3. **敵対的レビュー 1周目**: fresh context のレビュアーに、ファイル一式と `plans/regression-scenarios.md` を渡す（**ADR・レビュー履歴・変更の経緯は渡さない** — 敵対性を保つため）。**回帰シナリオを全件通してから**新規探索させる。指摘には領域タグ（`plans/findings-ledger.md` の語彙）と、**具体的な実行系列**を必ず付けさせる。severity: CRITICAL（壊れる・作業消失・ゲート素通り）/ MAJOR（実行系列を示せる誤動作・停止・人間の意図と違う結果）/ MINOR（実行系列を示せない曖昧さ）/ NIT
4. **修正する（1周目の CRITICAL / MAJOR / 回帰 NG）**。修正の前に必ず照合する:
   - `plans/findings-ledger.md` を領域タグで引き、**過去に同種の対応をしてそれが後の指摘を生んでいる（`その後` に `→` がある）なら同じ対応は禁止** — 畳む・削る・表にする、のどれかを選ぶ
   - `adr/` の決定と矛盾する指摘は、再検討の条件を満たすなら supersede、満たさなければ**却下**して ADR に履歴を1行
   - **レビュアーの修正案は症状の裏付けとして読む**。対応をそのまま採用しない
   - 直したら台帳に1行、回帰シナリオに1件追加する。MINOR / NIT は `plans/review-backlog.md` へ
5. **敵対的レビュー 2周目**（3 と同じ）。ここで出た指摘は**直さない** — 台帳と `plans/review-backlog.md` に記録し、**次にフレームワークを変更するときの入力**にする。例外は **CRITICAL と回帰 NG** だけで、これは直して3周目を回す（3周目も同じ扱い。4周目は無い — CRITICAL が残るなら変更を戻す）
6. 合格したら `plans/review-log.md` に記録し、**使う側の視点のレビュー**を1回行う（下記の観点。指摘は 4 と同じ規律で修正し、振る舞いを変えたら 5 をもう1周）
7. **構造に触る変更**（SKILL.md の共通手順・05 の最終受け入れ検証・reporting.md の書式・hotl-pm の区切り）を含む場合は、**実地ドライラン**を1回行う（サンプルプロジェクトで hotl を実際に走らせる）。机上レビューが出せない欠陥（実行環境・要件の書き方・人間の体験）はここでしか出ない。所見は `plans/` に記録し、CRITICAL / MAJOR は次の変更として扱う

レビュアーのモデルは既定（最上位）を使う。過負荷等でやむを得ず別モデルを使った周はその旨を記録する。

レビューで必ず通す観点:

- 全フェーズでの再開（セッション死→「続きから」）で作業消失・二重実行・成果物再生成が起きないか
- 承認ゲートの素通り経路（再開常套句「開発を進めて」等を承認と誤認しないか）
- sha256 ライフサイクル（何をいつハッシュし、以後の書き込みが記録を無効化しないか）
- ファイル間の相互参照の一致のうち**機械チェックでは捕まらないもの**（散文で言い換えられた節名・実行するコマンドの引数・記法の意味のずれ）。パスの実在と、代表的な記法で書かれた phase 名・state フィールド・Step 番号・節名は手順 2 が担う（手順 2 は網羅ではない — 言い換えられた参照は依然ここで見る）
- ターン終了規則の一貫性（許可リストは SKILL.md Step 6 が唯一の正）
- PM ↔ hotl の相互参照と委譲経路（マルチプロジェクト運用時: 区切り委譲・報告中継・2ライター禁止・ワークスペースガード）
- install.sh の全分岐

使う側の視点のレビュー（手順 6）で必ず通す観点:

- **やり取りの負担**: 人間が返事を求められる回数と1回あたりの分量が最小か。質問は答えられる形か（専門用語・内部用語で聞いていないか）
- **報告の読みやすさ**: フェーズ完了・中間・最終の各報告を読んだだけで「今どこにいて、次に自分が何をすべきか（何もしなくてよいか）」が分かるか
- **承認ゲートの判断材料**: 要約と要件定義書だけで、専門家でない人間が承認・修正依頼を判断できるか。提案由来の要件が区別できるか
- **止まった時の案内**: BLOCKED・キャップ超過・停止・不正状態の報告に、人間が取れる行動が具体的に書かれているか
- **成果物の受け取り**: 最終報告から起動方法・使い方・既知の制限・費用が分かるか。改善提案の採否をどう伝えればよいか分かるか
- **導入と初回**: README と install.sh だけで導入でき、最初の一言（要望）から hearing が始まるか。マルチプロジェクト（hotl-pm）の使い方が分かるか

この規律の理由: このフレームワークの品質は1回の執筆ではなく、敵対的レビューの反復で達成されてきた。初版から10ラウンド超のレビューで、CRITICAL 3件（承認ハッシュの自己矛盾、再開時の進捗台帳上書き、再開語によるゲート素通り）を含む60件超の欠陥がこのループで検出・修正されている（各ラウンドの修正は git 履歴の "Fix review ... findings" 系 commit を参照）。
