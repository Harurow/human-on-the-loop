# 秘密情報（接続情報・キー・トークンは必ずここに挙がる形式のファイルに置く）
.env
.env.*
*.pem
*.key
*.p12
*.secret
credentials.*
secrets.*

# Claude Code のローカル設定（個人の許可設定等を含み得る）
.claude/settings.local.json

# インストールされたスキル本体（フレームワーク側のリポジトリが正本）
.claude/skills/

# hotl-pm との受け渡し（一時ファイル）
docs/inbox.md

# 言語・ツールごとの生成物（技術選定後、開発の最初のタスクで実際に使うものを追記する）
# 例: __pycache__/ *.pyc .venv/ / node_modules/ dist/ / target/ / .pytest_cache/ .mypy_cache/

# OS / エディタ
.DS_Store
