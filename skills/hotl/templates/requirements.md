# 要件定義書: <app-name>

<!-- このテンプレートの使い方:
     - <> で囲まれた箇所を埋める。該当なしの節・行は「なし（理由）」と明記する（節ごと削除しない）
     - 要件は必ず ID を振り、第三者が読んで合否判定できる受け入れ基準を付ける
     - 「など」「適切に」「柔軟に」等の曖昧語は使わない -->

## 背景と目的

<なぜこのアプリが必要か。ヒアリングで判明した根本の課題（5-whys の到達点）>

## ユーザー像

<誰が・いつ・どんな状況で使うか>

## 用語

| 用語 | 定義 |
|---|---|
| <用語> | <このドキュメント内での意味> |

## 前提・制約

<!-- ユーザー側の所与の条件。設計の技術選定はこの範囲で行う（spec に転記され設計へ渡る）。無い種別は「なし」 -->

| 種別 | 内容 |
|---|---|
| 実行環境 | <OS・端末・ランタイムの制約。例: 自宅 Mac のスケジューラで実行し iPhone のブラウザで閲覧> |
| 既存資産 | <取り込む既存データ・置き換える既存スクリプト・移行の要否> |
| 外部サービス | <利用する API・サイトと、アカウントの有無・利用規約・レート制限の制約> |

## 機能要件

<!-- 出所: 「ユーザー」= ユーザーの要求由来、「提案」= エージェント提案でヒアリング中に採用されたもの。
     機能単位で判定する: 採用されたコンセプト内でも、元々ユーザーが要求していた機能は「ユーザー」 -->

| ID | 要件 | 受け入れ基準 | 優先度 | 出所 |
|---|---|---|---|---|
| R-1 | <要件> | <検証可能な合否条件> | must / should / could | ユーザー / 提案 |

## 非機能要件

<!-- 必須5観点（性能目標・費用上限・セキュリティ前提・データ保全・運用）は必ず1行ずつ書く。該当しなければ要件を「なし（理由）」、
     受け入れ基準は検証可能な形（例: 費用上限「なし（外部課金なし）」→「課金対象の外部呼び出しがコードに存在しない」）、
     書けなければ「該当なし（理由）」とする。後工程は ID でなく「観点」列で照合するので、差分更新では既存 NR の ID を維持し
     不足する観点の行を追記する -->

| ID | 観点 | 要件 | 受け入れ基準 | 出所 |
|---|---|---|---|---|
| NR-1 | 性能目標 | <想定データ量と数値目標。例: 記事1万件の状態で一覧表示2秒以内・通常実行5分以内> | <計測方法と合否条件> | ユーザー / 提案 |
| NR-2 | 費用上限 | <外部 API・課金サービスの上限と想定実行頻度。例: 月額 $30 以内（1日6回の定期実行を想定）・1実行 $1 以内。課金要素が無ければ「なし（外部課金なし）」、上限を設けないなら「上限なし（実測の開示のみ）」> | <実測の方法。例: 1実行あたり実測 × 想定実行頻度（1日 n 回）の月額換算が上限内> | ユーザー / 提案 |
| NR-3 | セキュリティ前提 | <公開範囲（ローカルのみ / 家庭内 LAN / インターネット）・扱うデータの機微度・認証の要否> | <攻撃面の棚卸しで前提を超える露出がない> | ユーザー / 提案 |
| NR-4 | データ保全 | <消えて困るデータか・バックアップの要否と方法・保持期間。例: SQLite を日次で外部ディスクへコピー、1年保持。再取得可能なキャッシュだけなら「なし（消失許容）」> | <例: 手順どおりにバックアップから復元でき、一覧が一致する> | ユーザー / 提案 |
| NR-5 | 運用 | <失敗の検知手段と更新方法。例: 定期実行の失敗時に通知が届く・ログを docs で指定した場所に残す。手動実行のみなら「なし（エラーは標準出力で確認）」> | <例: 意図的に失敗させて通知が届く> | ユーザー / 提案 |
| NR-6 | <その他: 可用性・互換性・使用性等> | <要件> | <検証可能な合否条件> | ユーザー / 提案 |

## 非ゴール

<今回は作らないと合意したこと。スコープクリープ防止のため必須>

- <非ゴール 1>

## 成功基準

<このアプリが「成功した」と言える状態。ユーザー自身の言葉に基づくこと>

## 承認記録

<!-- 承認者・日時のみをここに記入する。記入後、このファイルには一切書き込まないこと。
     対象版の sha256 は docs/hotl.state.json の approval に記録される（このファイルに書くとハッシュが自己矛盾する） -->

| 項目 | 値 |
|---|---|
| 承認者 | 未承認 |
| 承認日時 | 未承認 |
