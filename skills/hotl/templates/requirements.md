# 要件定義書: <app-name>

<!-- このテンプレートの使い方:
     - <> で囲まれた箇所を埋める。該当なしの節は「なし」と明記する（節ごと削除しない）
     - 機能要件は必ず ID (R-1, R-2, ...) を振り、受け入れ基準を付ける
     - 受け入れ基準は「検証可能」な形で書く（誰が読んでも合否判定できること）
     - 「など」「適切に」「柔軟に」等の曖昧語は使わない -->

## 背景と目的

<なぜこのアプリが必要か。ヒアリングで判明した根本の課題（5-whys の到達点）>

## ユーザー像

<誰が・いつ・どんな状況で使うか>

## 用語

| 用語 | 定義 |
|---|---|
| <用語> | <このドキュメント内での意味> |

## 機能要件

<!-- 出所: 「ユーザー」= ユーザーの要求由来、「提案」= エージェント提案でヒアリング中に採用されたもの。
     機能単位で判定する: 採用されたコンセプト内でも、元々ユーザーが要求していた機能は「ユーザー」 -->

| ID | 要件 | 受け入れ基準 | 優先度 | 出所 |
|---|---|---|---|---|
| R-1 | <要件> | <検証可能な合否条件> | must / should / could | ユーザー / 提案 |

## 非機能要件

<!-- 性能目標・費用上限・セキュリティ前提の3観点は必ず1行ずつ書く（該当しない場合も「なし（理由）」と
     受け入れ基準を書いて空欄にしない。最終受け入れ検証の非機能横断チェックがこの3行を「観点」列で照合する）。
     ID は既存プロジェクトの差分更新では振り直さず、既存 NR を維持したまま不足の観点を追記する（照合は ID でなく観点で行う）。
     「なし」の行の受け入れ基準の例: 費用上限「なし（外部課金なし）」→「課金対象の外部呼び出しがコードに存在しない」。
     「なし」の NR は仕様のトレーサビリティ表で「該当なし」と記し、対応仕様ゼロ確認の対象外にする -->

| ID | 観点 | 要件 | 受け入れ基準 | 出所 |
|---|---|---|---|---|
| NR-1 | 性能目標 | <数値で。例: 通常実行は5分以内・一覧表示は2秒以内> | <計測方法と合否条件> | ユーザー / 提案 |
| NR-2 | 費用上限 | <外部 API・課金サービスの上限。例: 月額 $30 以内・1実行 $1 以内。課金要素が無ければ「なし（外部課金なし）」> | <実測の方法。例: 1実行あたり実測 × 想定実行頻度（1日 n 回）の月額換算が上限内> | ユーザー / 提案 |
| NR-3 | セキュリティ前提 | <公開範囲（ローカルのみ / 家庭内 LAN / インターネット）・扱うデータの機微度・認証の要否> | <攻撃面の棚卸しで前提を超える露出がない> | ユーザー / 提案 |
| NR-4 | <その他: 運用・可用性・互換性等> | <要件> | <検証可能な合否条件> | ユーザー / 提案 |

## 非ゴール

<今回は作らないと合意したこと。スコープクリープ防止のため必須>

- <非ゴール 1>

## 成功基準

<このアプリが「成功した」と言える状態。ユーザー自身の言葉に基づくこと>

## 承認記録

<!-- 承認者・日時のみをここに記入する。記入後、このファイルには一切書き込まないこと。
     対象版の sha256 は docs/hotl.state.json の approval に記録される（このファイルに書くとハッシュが自己矛盾する） -->

| 項目 | 値 |
|---|---|
| 承認者 | 未承認 |
| 承認日時 | 未承認 |
