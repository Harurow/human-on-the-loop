# 実地ドライラン2 — stock-memo（再開シナリオ） FINDINGS

- 開始: 2026-09-06 01:45 JST（`date` 実測）
- 起点: phase `requirements`、セルフ整合性チェック3周目「着手」のみ記録・完了記録なし。requirements.md 未コミット、hearing-notes.md / log.md に未コミット差分あり
- インストール版: `.hotl-version` = 0.9.0 (commit 57539e6, copy)
- 実行モデル: セッションモデル（Fable 5.1）。サブエージェントは Agent ツールで呼び出し（モデル未指定＝継承）

## 記録の凡例
- 欠陥: `[SEV] F-n <ファイル>「<節>」 — 何が起きたか / 素直な解釈`
- 再開の観察: `[RESUME]`
- 実測: `[METRIC]`
- 人間役の摩擦: `[HUMAN]`
- 選択式 UI: `[ASK]`

---

## 0. セッション起動〜再開判定（Step 1〜4）

[RESUME] 起動メッセージ「続きから」として SKILL.md Step 1→2→4→5 を逐語実行。判定の経過:
- Step 1: reporting.md を Read
- Step 2: cwd に `docs/hotl.state.json` あり、`.claude/skills/hotl-pm` なし、ワークスペースガード非該当 → cwd がプロジェクト
- Step 4 自己修復: log.md あり / `git rev-parse --show-toplevel` = プロジェクトルート / `.gitignore` あり → 補修なし
- Step 4 文脈復元: log.md 末尾を読み、`phase_history` 末尾 `requirements` に対応する遷移報告「ヒアリング完了 → 要件定義へ」あり → 補記なし。`docs/lessons.md` なし
- Step 4 ハッシュ照合: `approval.approved` = false → 対象外
- Step 4 inbox: `docs/inbox.md` なし。trail 末尾に対応記録の無い interrupt/finding なし（1周目14件・2周目9件とも「修正済み」あり）
- Step 4 不変条件: phase `requirements` & approved=false → 正常
- Step 5: phase `requirements` → 02「要件定義中の再開」。起動メッセージは単なる再開語 → 書きかけ成果物の確認（requirements.md にガイドコメント・`<...>` 残存なし＝完成形）→ 未反映の修正依頼・要件矛盾 finding なし → 続きはセルフ整合性チェック
- 周回数の判定: 「各周の開始・完了を trail に report で記録し、再開時はそこから周回数を数える（完了の無い開始はその周をやり直す）」→ 3周目着手のみ・完了なし → **3周目をやり直す**（キャップ最終周）。起算点は修正依頼・承認リセットのエントリが無いので要件定義開始
- 未コミットの扱い: SKILL.md / 02 に「対話フェーズ中の未コミット成果物」に関する規定は無い（commit は Step 6 の遷移時のみ）。requirements.md が untracked のまま作業を継続するのは規定どおりと解釈し、何もしない
- 結果: 作業の消失なし・二重実行なし・成果物の再生成なし（requirements.md をそのまま使う）

[MINOR] F-1 02-requirements.md「セルフ整合性チェック」 — 「2周目以降は前回の指摘リストと修正差分も併せて渡す」の「修正差分」が再開後は再構成できない。requirements.md は遷移まで commit されず、周ごとの版も保存されないため、クラッシュ後に残るのは log.md の対応記録の要旨だけ。素直な解釈: 対応記録の要旨（「修正の要旨」）を「修正差分」の代わりに渡す。実害は小さいが、「差分」を渡せと書いてあるものを渡せないので規定と実態がずれる

[NIT] F-2 02-requirements.md「セルフ整合性チェック」 — 「前回の指摘リスト」が直前の1周のみか累積かが不明。3周目に1周目の指摘14件を渡さないと同一指摘の再発見を防げない可能性がある。素直な解釈: 直前の周（2周目）のみを渡す（字義どおり）。ただし念のため1周目の対応要旨は含めない

[NIT] F-3 SKILL.md「Step 4」 — 「状況確認クエリ」の場合 inbox を読むだけと書かれているが、起動メッセージが再開語の場合と状況確認の場合の判定を Step 4 の前に行う指示が明示的に無い（Step 5 に書かれている）。読む順序として Step 4 で「状況確認クエリなら〜」と参照される時点で判定基準は Step 5 にあるため、一度先読みが必要。実害なし


## 1. 要件定義（セルフ整合性チェック3周目やり直し〜ゲート）

[METRIC] 3周目レビュー: サブエージェント1体、tokens 67,364、所要 238 秒、指摘 12 件（FAIL）。2周目までで累計 23 件修正済みの文書に対して、さらに 12 件出た（傾向: 境界条件・未定義の入力・全称基準の細部）
[METRIC] 3周目の修正: requirements.md へ Edit 15 箇所、hearing-notes.md へ 2 箇所。修正に約 8 分

[MINOR] F-4 02-requirements.md「セルフ整合性チェック」 — キャップ最終周（3周目）が FAIL だったとき、「指摘を修正してよいが再チェックしない」のか「修正せず残指摘として開示する」のかが読み取れない。「修正した指摘には修正済み、反映せず残した指摘にはゲートで開示」は周ごとに適用されるので修正は許されると読めるが、修正後の版は誰にもレビューされないまま承認ゲートに出る。素直な解釈: 12件すべて修正し、「3周目の修正が未レビュー」であることをゲートで開示。reporting.md「収束しなかった指摘の開示の型」(3) は「反映したか未反映か」を書かせるので、両方あり得る前提のようだが、02 側には書かれていない

[MINOR] F-5 02-requirements.md「セルフ整合性チェック」/ checklist-requirements.md — 3周かけても収束しない構造。チェックリスト §2〜§3 は「境界条件の未定義」を無限に生成できる（空白のみ・全角数字・ファイル不在・CR…）。2周目9件→3周目12件と減っていない。fresh context のレビュアーは毎回別の角度から未定義を探すので、「PASS するまで反復」は現実には終わらず、キャップ到達→ゲート開示が常態になる。CLI 5コマンドの最小アプリでこれなので、実用アプリでは要件定義書が肥大化する（現在 84 行→この周で約 90 行、1行あたりの基準が非常に長い）。素直な解釈: 規定どおりキャップで打ち切る

[MINOR] F-6 02-requirements.md「執筆」3 — レビュアー指摘12（Python の導入手順）への対応で、前提に「macOS 12 以降」という新しい既定値を置いた。02 は「既定値を置いて出所を提案にし、ゲートで提案由来として明示」と言うが、前提・制約の表には「出所」列が無い（機能・非機能の表にはある）ため、提案由来を本文中の括弧書きで示すしかない。テンプレート（templates/requirements.md）の前提・制約表に出所列が無いのが原因と推測

[NIT] F-7 02-requirements.md「執筆」4 — 「要件の追加・削除・変更を反映する場合は hearing-notes の該当節も更新する」の対象に、レビュー指摘による「明確化」（意味を変えない補足）が含まれるかが不明。素直な解釈: 意味が変わったもの（メモ消去、Python 前提）のみ hearing-notes を更新

[ASK] 承認ゲート: AskUserQuestion はこの実行環境（Agent ツールで起動されたサブエージェント）では使えないため、reporting.md の規定どおりテキストで3択を提示。選択肢「承認する / 修正を依頼する / 質問がある」は答えやすい。ただし本文が長い（要約 5 行＋前提 7 行＋開示 3 行＝約 20 行）。02 の「要約10行以内」は守れるが「非機能・前提の各行を平たく1行ずつ」で 7 行増える。ペルソナ（非エンジニア）にとって「Unicode コードポイント順」「RFC 4180」等は要約に出さなかったので読めるが、requirements.md 本体を開いたら読めないだろう
[HUMAN] ゲート本文で一番迷うのは「こちらで置いた前提」の7行。佐藤さんの立場では「.bak」「python3 --version」がすでに分からない。ペルソナ規定どおり「おまかせ」で承認
[METRIC] 承認ゲート提示までの人間への出力: 1 回（約 20 行）。人間の入力: 1 回（「承認します」）
[RESUME] ここでターンが終わる想定。次の起動メッセージ「承認します」で Step 1〜5 を再実行し、02「承認待ちからの再開」で判定する

## 2. 承認〜仕様化

[RESUME] 起動メッセージ「承認します」: Step 4 で phase_history 末尾 `awaiting_approval` の遷移報告あり・approved=false・inbox なし → 02「承認待ちからの再開」。停止・状況確認でない → 「承認の判定と記録」: 明示的同意・phase awaiting_approval・log 上でゲート提示エントリより後・修正の混在なし・直近の対応記録は「本文待ち/再確認中」でない → 承認。手順 1〜5 を順に実行（interrupt 記録 → approval 記録 → requirements.md 承認記録節 → sha256 → state → Step 6）。commit `456a13f`
[METRIC] 承認処理: 人間への出力 1 回（5 行＋介入案内 1 行）。所要 約 1 分

[MINOR] F-8 SKILL.md「共通手順: 人間の入力の経路」手順1 と 02「承認されたら」手順1 — ゲート応答「承認します」を trail に `interrupt` として記録するかが二重に規定されている。共通手順は「処理を始める前に interrupt として記録」と言い、02 の承認手順は「log.md に承認を approval として記録（処理の前に）」と言う。素直な解釈: 両方書く（interrupt → approval → 対応記録「回答として処理」）。結果、承認1回で trail に3エントリ増える。実害は無いが冗長で、どちらか一方の記録で足りるなら明示してほしい

[NIT] F-9 02「承認されたら」手順2 — 承認日時は `date +"%Y-%m-%dT%H:%M:%S%z"` の実行結果を requirements.md に書く。手順4で state に書く `approved_at` は別の `date` 実行になり得るので、requirements.md の承認日時と state の approved_at が秒単位でずれうる（今回は同秒だったが）。監査で「同じ承認か」を突き合わせる際に混乱の元。素直な解釈: 同一の値を使う（1回の date 結果を両方に使うのが安全）

## 3. 仕様化・設計（自律区間）

[METRIC] 仕様化: サブエージェント1体、tokens 94,126、所要 541 秒、spec.md 376 行 / 36KB（S-1〜S-12、判断 D-1〜D-22）。メインループ検算 OK・再実行なし
[METRIC] 設計: サブエージェント1体、tokens 79,564、所要 543 秒、design.md 111 行（プロンプトで「100 行前後」と指示した）。検算 OK・再実行なし
[METRIC] 承認〜開発開始までの人間への出力: 2 回（承認直後の報告 1、仕様化完了・設計完了はテキストで流すが返信不要）

[MAJOR] F-10 03-specification.md「進め方」 — 「文書の深さはプロジェクト規模に比例させる。機能要件5件以下の小規模なら S-n は R-n と1対1で簡潔に」とあるが、機能要件8件・受け入れ基準が詳細な要件定義書を渡すと、サブエージェントは 376 行・22 個の判断記録を書いた。要件定義書の詳細さ（3周のレビューで肥大化）がそのまま仕様書の肥大化に伝播する。長さの上限（行数・S-n 数の目安）が無く、「簡潔に」は fresh context のサブエージェントに効かない。実害: 設計サブエージェントに渡す際に「インラインで渡せない大きさ」になり、パス渡し＋読取制限に切り替えた（SKILL.md の規定内だが分岐が増える）。開発フェーズでも spec.md 全体を把握するコストが上がる。素直な解釈: 規定どおり受け入れる（再実行の条件は「検算で漏れ」のみで、長すぎは再実行の理由にならない）

[MINOR] F-11 03/04「進め方」1 — 「`<...>` の記入欄が残っていれば書きかけ」の判定と、「コード引用の中に入れて区別」の規則が、フェンス付きコードブロック（``` 〜 ```）に言及していない。spec.md 197 行目 `python3 stock.py <サブコマンド> -h` と design.md の構成図 `<add|list|...>` `<stock.py の隣>` はコードブロック内で、バッククォートの行内引用ではない。素直な解釈: コードブロックもコード引用とみなして書きかけ判定から除外。機械的に `grep '<[^>]*>'` で検査する監査者は誤検出しうる

[MINOR] F-12 04-design.md「実行形態」 — 設計サブエージェントに渡す入力リストに「開発環境の事実（Python のバージョン・ネットワーク可否・pip の可否）」が無い。spec の前提・制約（利用者の環境）と開発環境は別物で、今回は「pip install が使えない」という開発環境側の制約が技術選定（formatter/型検査を導入しない）を左右した。私はプロンプトに「環境の事実」を独断で追加した。書かれていなければサブエージェントは black/ruff/mypy/pytest を選び、T-1 環境構築で初めて破綻する。素直な解釈: メインループが把握している事実を追加で渡す（入力リストは「これだけ」と書かれているので規定違反の可能性）

[NIT] F-13 SKILL.md「渡す」の意味 — 「大きすぎる場合」の閾値が無い。36KB を大きすぎると判断したのは私の裁量

## 4. 開発（T-1〜T-2）

[METRIC] T-1: 所要 約 2 分、レビュー省略（雛形のみ）。T-2: 所要 約 10 分、コードレビュー 2 回（サブエージェント 2 体、tokens 58,602 + 60,761、所要 120 秒 + 186 秒）、指摘 3 件 → 1 件
[MINOR] F-14 05-development.md「Step 2」5 — 「①の修正後の再レビューは1周まで（収束しない指摘は②か③へ）」。再レビューで**新しい**指摘（テストのみの欠陥）が出た場合、①として直してよいのか（再レビュー無しで）②に回すべきかが読み取れない。②に回すと「テストの回帰ガードが効かない」だけのために修正タスクを挿入し、最終検証の前に再びレビューが要る。素直な解釈: ①として修正し再レビューはしない（規則は再レビューの回数を制限しているのであって修正を禁じていない）。trail に理由を書いた
[MINOR] F-15 05-development.md「Step 2」5 — レビューに渡す「タスクの diff」を `git diff --no-index /dev/null <file>` で作れと言うが、サブエージェント（Agent ツール）へはプロンプトに貼るしかなく、diff 形式（+ 行）で貼ると読みにくいため全文を貼った。変更ファイルの全文と diff のどちらを渡すかで、レビュアーの見え方が変わる（全文だと「変更されていない箇所」も指摘対象になる）。実害なし
[NIT] F-16 checklist-code.md — 「備考: 判定に含めない付記」の扱いは 05 で「処理は不要」とされるが、今回の備考 (a)(b) は次タスクの実装で必ず守るべき事項だった。捨ててよい情報ではないが、記録先が無い。素直な解釈: 対応記録の本文に書き残した

## 5. 開発（T-3〜、中間報告、状況確認の割り込み）

[METRIC] T-3: 所要 約 12 分、コードレビュー 2 回（tokens 72,531 + 54,862、所要 279 秒 + 119 秒）、指摘 5 件 → 1 件（却下）
[MAJOR] F-17 05-development.md「Step 2」5 / SKILL.md「渡す」の意味 — レビュアー（サブエージェント）へ「diff をインラインで渡す」と、プロンプト経由でエスケープ文字（`﻿`）が生文字に変換されて見え、**実ファイルには無い欠陥が指摘された**（T-3 再レビューの唯一の指摘が誤指摘）。私は decision で却下したが、素直なエージェントが指摘を信じて「修正」すると、存在しない問題を直すために無駄なループが起きる。インライン渡しはテキスト変換に弱く、正確さが要る diff は「ファイルパス＋読取制限」で渡す方が安全。規定は「原則インライン」で逆
[MINOR] F-18 reporting.md「開発中間報告の書式」— 「タスク3件完了ごと」の報告は、T-1〜T-3 完了時点で出した。人間役としては 3 タスクの名前（環境構築・共通規則…）が内部用語で、何ができたか分からない。「あなたの作業はありません」も書式に含まれないので、返事が要るのか迷う
[HUMAN] 「いまどうなってる？」への返答は 05/reporting.md の割り込み手順 0-2 どおり 1〜2 行で答えて続行。摩擦なし。ただし佐藤さんの立場では「5つのコマンド本体」が何を意味するかは分からない（「登録や一覧のコマンド」と言うべきだった）
[ASK] 開発中は選択式の場面なし（正しい: 非ブロッキング）

## 6. 開発（T-4〜T-5）

[METRIC] T-4: 所要 約 13 分、レビュー 2 回（tokens 75,677 + 70,237、所要 222 秒 + 169 秒）、指摘 6 件（判定 PASS と書かれた）→ 0 件。T-5: 所要 約 11 分、レビュー 2 回（tokens 76,080 + 66,050、所要 211 秒 + 176 秒）、指摘 4 件 → 2 件（①で修正・3 回目なし）
[MINOR] F-19 checklist-code.md「判定の出力書式」— T-4 の 1 回目レビューは「判定: PASS」なのに指摘 6 件を返した（規則: 1 件でもあれば FAIL）。レビュアーが「MINOR/NIT だから PASS」と重み付けした。チェックリストに重大度の概念が無いので、PASS/FAIL を指摘の有無だけで決める規則が守られない。素直な解釈: 指摘があるので FAIL 相当として処理した
[MINOR] F-20 05-development.md「Step 2」5 — レビュー 2 回目が「2周目ルール（CRITICAL・回帰 NG 以外は台帳記録）の対象と考える」と書いてきた。レビュアーはフレームワーク本体の PRINCIPLES（有界化）を知らないはずだが、指摘の重み付けを勝手に始めている。これは fresh context のレビュアーへの入力に「再レビュー（2回目）」と書いたことで誘発された。**05 のコードレビューには「2周目の指摘の扱い」が規定されていない**（要件レビューは 02 に「ゲートで開示」がある）。①で直すか ② に回すかは私の裁量になった（今回は①・再レビューなし）
[MINOR] F-21 04-design.md / 05 — 設計書の許可リストに `typing` が無く、実装で使ったため T-5 で design.md を追随更新した。「修正が spec/design と食い違うなら同じタスクで両方更新」（reporting.md 3分類①）の規定どおりだが、design.md の更新は T-5 の commit に混ざり、後の整合性監査が「docs の変更 commit」として tasks.md 整合を要求する可能性がある（05 Step 1 の判定は `git log -- docs/design.md` が tasks.md の最終 commit より後かを見る。T-5 の commit には tasks.md も含まれているので同一 commit → 単純再開扱いで済むはず。検証は次の再開時）
[NIT] F-22 レビュアーが毎回「行長 100 桁」を文字数か表示幅かで解釈を揺らす。design.md に「文字数」と書けば済むが、テンプレートの検証ツールチェーン表には単位の欄が無い

## 7. T-6 周 1（監査・UX・セキュリティ・受け入れ）→ UX 由来の承認リセット → 要件差分更新 → ゲート再提示

[METRIC] T-6 周 1: サブエージェント 4 体を並行起動（監査 114,630 tokens / 253 s、UX 87,546 / 399 s、セキュリティ 99,082 / 277 s、受け入れ 107,260 / 318 s）。壁時計 約 7 分。結果: 監査 FAIL（MAJOR 1・MINOR 8）、UX 必ず直すべき 1・次イテレーション 7・良い点 7、セキュリティ PASS（MINOR 8）、受け入れ 全合格（NR-1 実測 0.026〜0.028 s）
[METRIC] 承認リセット後の要件レビュー 3 周: 77,580 / 318 s、74,167 / 264 s、71,087 / 222 s。指摘 15 → 10 → 9（合計 34。修正 17・開示 17）。要件定義（差分更新〜ゲート再提示）の所要 約 20 分。requirements.md 87 行（NR-7 の 1 行が約 1,500 文字）

[MAJOR] F-23 02-requirements.md「セルフ整合性チェック」× 05 1.5「UX 由来の承認リセット」 — 承認済み文書に NR を 1 行足すだけの差分更新でも、規定は「requirements.md 全文」を fresh レビュアーに渡すため、**承認済み本文への新規指摘が周ごとに 10 件前後出続ける**（4〜12・14・15、2・10、2・3・5・9）。承認済み本文を動かすと「承認した内容と違う」ことになるので私は「ゲートで開示」に回したが、02 にその方針（差分更新時は変更箇所とその周辺に限る、承認済み本文の指摘は開示）が無く、素直に従えば 3 周とも全文を直して承認済み文言が大きく動く。どちらの解釈でも人間へのゲート提示に「17 件未反映」のような不安な開示が付く。UX ウォークスルーで 1 件見つかるたびにこのコスト（約 20 分・約 22 万 tokens）がかかる
[MAJOR] F-24 05-development.md「1.5 UX ウォークスルー」 — 「必ず直すべき」のスコープ内外の判定基準「受け入れ基準・仕様に反する、または新機能に当たる」が、**要件が許容形（「英語でよい」）で書かれている場合**に決められない。書式エラーの日本語化は既存基準に反しないが NR-6 の限定列挙の外にある。私は「NR-6 の限定列挙の外＝スコープ外」と読んで承認リセットに進んだが、「反しないのでスコープ内・②でタスク化」とも読める。後者なら人間往復なしで 10 分で終わる。判定基準に「要件が明示的に限定している範囲の外なら要件変更」と書くか、UX 由来の小さな改善は②に倒す既定を置くべき
[MAJOR] F-25 reporting.md「対応の記録」/ 05 整合性監査 — 前セッションが書いた対応記録（21:59）の `関連:` が finding の見出し（21:58）ではなく着手 report（21:53）を引用していた。**逐語引用の誤りは書いた時点では検出されず、最終監査で MAJOR として初めて出る**。今回は監査が拾ったが、同じ誤りを再発防止する機械検査（対応記録の関連が既存見出しと一致するか）が無い。素直な対処: 正しい見出しで対応エントリを追記（旧エントリは追記専用のため残す）
[MINOR] F-26 05「最終受け入れ検証」役割ごとの規則 — 「監査 → UX → 検証の順」と書かれているが、同じ commit に対する独立した読み取りなので並行起動した（壁時計 7 分 vs 逐次なら 21 分）。規定に「並行してよい」とも「逐次」とも書いていない。素直な解釈: 処理順（指摘の処理）は守り、起動は並行
[MINOR] F-27 checklist-security.md / 05 手順2 (a) — セキュリティ MINOR 8 件は「受け入れ検証が全合格の周では最終報告で開示に固定」の規則で開示行きになった。うち 5（シンボリックリンク経由の起動で在庫が二分）と 3（0644 で他アカウントから読める）は 1 行の修正で直るが、規則が修正を止める。今回は承認リセットで再度開発に戻るので、そこで直せば「再検証を呼ぶ MINOR 修正」にはならないが、規則上は開示に固定されたまま。次周で修正してよいかが不明
[MINOR] F-28 05 1.5 — UX レビュアーには「スクリーンショットの代替としてテキスト記録を docs/ux/ に保存」させたが、その保存先が `docs/` 配下のため、監査者の「docs/ 一式」の入力に 13 ファイルの記録が混ざる（監査者は読んでいなかったが、量が増えると監査の入力を圧迫する）。また、この記録は Step 6 の commit 対象として「docs/ 配下」に含まれ、git 履歴に利用者データ相当（UX 検証で入れた 100 品目の架空データ）が入る
[MINOR] F-29 SKILL.md「共通手順: 承認リセット」手順1 — 「旧承認記録（承認者・日時・ハッシュ）を log.md に approval として退避」は書けたが、**退避先のフォーマットが決まっていない**（本文の書き方が自由）。後の監査が「承認リセットの記録」を機械的に読むなら形式が要る
[HUMAN] ⏪ 報告は「これまでの実装は引き継ぎます」があるので安心できるが、「利用者視点の検証で見つかった…」の後にゲートが来るまで（実際には 20 分）人間は待たされる。同一ターン内で進むので体感は 1 メッセージだが、ゲート本文に「17 件未反映」の開示が付くと、非エンジニアは「大丈夫なのか」と不安になる
[ASK] 承認ゲート（2 回目）: 選択肢 4 つ（追加案を採用して承認 / 見送って承認 / 修正 / 質問）はペルソナ規定「そのまま承認」に対して「採用」と「見送り」のどちらが「そのまま」か曖昧。推奨を添えないと非エンジニアは選べない（02 には推奨を書けとは無い）。人間役としては「採用して承認」を選ぶ

## 8. 2回目の承認〜仕様化（差分更新）

[METRIC] 承認処理（2 回目）: 約 1 分。仕様化差分: サブエージェント 1 体、tokens 132,627、所要 689 秒（初回の 541 秒より長い）。spec.md 376 → 528 行（S-13 が約 100 行: 文言表 A/B/C・F0〜F6・G1〜G3）
[MAJOR] F-30 03-specification.md「実行形態」差分更新 — NR 1 行（約 1,500 文字）の追加が、仕様書 152 行の追加に膨らんだ。差分更新でも「文書の深さは規模に比例」が効かず、サブエージェントは受け入れ基準の粒度をそのまま仕様に展開して文言表を作る。UX 由来の小さな改善 1 件で、要件 20 分 → 仕様 12 分 → （設計・実装・再検証がこれから）という工数になり、P7 Lean と食い違う。素直な解釈: 規定どおり（再実行の条件は検算漏れのみ）
[MINOR] F-31 03「実行形態」— 差分更新の入力「変更点の要約（メインループが log.md・会話から把握しているもの）」は、私が手で書いた 3 項目の要約。要約の質が仕様の質を決めるが、書き方の指針（どの粒度で、受け入れ基準を含めるか）が無い。今回は要件定義書全文も渡すので実害は薄いが、要約と要件定義書が食い違った場合の優先順位も書かれていない
[RESUME] 2 回目の承認: 02「承認待ちからの再開」の判定は 1 回目と同じ経路で問題なし。UX 由来の 4 択で「採用して承認」→ 02 の「採用なら元の finding に修正済み」を付けた

## 9. 設計差分 → バックログ整合 → T-7 → T-6 周 2

[METRIC] 設計差分: サブエージェント 1 体、tokens 107,231、所要 342 秒。design.md 112 → 121 行。バックログ整合: 約 1 分（判定・T-7 挿入・カバレッジ・commit）。T-7 実装: 約 12 分、レビュー 2 回（90,603 / 228 s、67,719 / 140 s）、指摘 3 → 0。テスト 83 → 94 件
[METRIC] T-6 周 2: 4 体並行（監査 158,484 / 492 s、UX 82,327 / 314 s、セキュリティ 106,878 / 312 s、受け入れ 110,716 / 318 s）。壁時計 約 8 分。監査 FAIL（MINOR 11・新規）、UX 必ず直すべき 0・次イテレーション 6、セキュリティ PASS（MINOR 2）、受け入れ 全合格
[METRIC] UX 1 件（書式エラーの日本語化）に要した総コスト: 承認リセット → 要件 3 周（約 20 分）→ ゲート → 仕様差分（12 分）→ 設計差分（6 分）→ 整合（1 分）→ T-7（12 分）→ 再検証（8 分）= **約 60 分・サブエージェント 11 体・約 100 万 tokens**。1 回目の done 到達見込み（03:20 頃）から 1 時間遅れた

[MAJOR] F-32 05「整合性監査」× 再周 — 監査 2 周目が「新規のみ」でも 11 件（MINOR）出た。うち 5 件（4〜8）は **trail（log.md）の書き方への指摘**（対応語彙の時点・decision の書式・commit の同定・「対象 commit」の基準・周番号の数え方）で、成果物の品質とは無関係。監査者に playbook を渡さない規定（05 手順1 の入力は docs/ 一式・git log・「対応の記録」節のみ）のため、周番号の規則（単調増加）を知らずに「食い違い」と判定し、却下の手間が増える。監査入力に「周の記録」の規則を含めるか、trail の形式を機械検査する軽い手段（対応記録の関連が見出しと一致するか、周番号の単調増加）に置き換えるべき
[MAJOR] F-33 05「役割ごとの規則」— 周 2 で受け入れが全合格し、UX の「必ず直すべき」がゼロになったので done に進めたが、**周 1 で見つかっていたセキュリティ MINOR 8 件（うち 1 行で直るもの 2 件）は「開示に固定」の規則により 2 周とも修正されず**、承認リセットで開発に戻った間（T-7）も直せなかった（規則上 T-7 はスコープ外）。「再検証を呼ぶ MINOR 修正を避ける」規則の副作用として、**人間が再承認で開発に戻したときが MINOR を直す唯一の機会なのに、そのときも直せない**。承認リセット後のバックログ整合で「開示予定の MINOR のうち低リスクなものを②に載せてよい」と書くべき
[MINOR] F-34 05 1.5「UX ウォークスルー」の再周 — 「前回の指摘を再発見しない」と指示しても、新規の「次イテレーション」が 6 件出た（UX は毎回別の角度で改善点を見つける）。UX の proposal は最終報告に列挙されるが、周を重ねるほど積み上がり（7 + 6 = 13 件）、最終報告が長くなる。要約の規則（reporting.md「改善提案は原則 2〜5 件」）と食い違う
[MINOR] F-35 T-7 の実装時、`git diff` を渡すレビューでも「表 A に無い組み合わせで KeyError」のような防御的欠陥が見つかった（レビューは有効）。一方で「クラス属性の可変 dict」「open().read() 未クローズ」のような構造・作法の指摘も一定数あり、formatter/linter を導入できない環境（pip 不可）ではこれらの指摘がレビュー工数を食う。design の「formatter しない」判断のコストが見えた
[RESUME] 承認リセット後のバックログ整合の判定「tasks.md の最終 commit より後に docs の変更 commit がある」は正しく効いた（整合が必要 → T-7 挿入 → T-6 は [ ] のまま再追記なし）。ただし私は「tasks.md の最終 commit」を誤って書いた（監査が指摘）。判定自体は git log で機械的にできるのに、人手で hash を書かせる規定が誤記を生む
[HUMAN] 周 2 では人間への出力なし（自律区間）。承認から done までの間、人間には「✅ 承認待ち 完了 → 仕様化へ」以降、仕様化・設計の ✅ とバックログ整合以外に何も届かない。40 分以上無音（実際は同一ターン）で、開発中間報告（3 タスクごと）も T-7 が 1 タスクだけなので出ない

## 10. done 到達

[METRIC] **done 到達: 2026-09-06 04:24（再開 01:45 から 2 時間 39 分）**。git commit 29（再開後 27）。docs/: requirements 87 行 / spec 529 行 / design 121 行 / tasks 9 行 / hearing-notes 70 行 / log 519 行 / lessons 9 行 = 1,344 行。コード: stock.py 約 530 行、tests 3 ファイル 約 900 行、テスト 94 件
[METRIC] サブエージェント呼び出し: 合計 **25 体**（要件レビュー 6、仕様化 2、設計 2、コードレビュー 11、監査 2、UX 2、セキュリティ 2、受け入れ 2）。tokens 合計 約 2.2M（サブエージェント分のみ）
[METRIC] 人間への出力（ターン終了を伴うもの）: ゲート 2 回、最終報告 1 回。返信不要の報告: ⏪ 1 回、✅ 6 回、🔨 2 回、状況確認への返答 1 回。人間の入力: 承認 2 回、状況確認 1 回
[METRIC] フェーズごとの所要（再開後）: 要件定義（3周目やり直し〜ゲート）11 分 / 承認 1 分 / 仕様化 11 分 / 設計 10 分 / 開発 T-1〜T-5 47 分 / T-6 周 1 10 分 / 承認リセット〜再ゲート 20 分 / 承認 1 分 / 仕様差分 12 分 / 設計差分 7 分 / 整合＋T-7 13 分 / T-6 周 2 12 分 / done 2 分
[MAJOR] F-36 05「完了」最終報告 — 求められる項目が 10 個（何ができたか／最初にすること／使い方／既知の制限／受け入れ結果／非機能横断／UX 結果／改善提案／採否の伝え方／凍結中の指摘と却下）で、書式どおり書くと約 45 行になる。reporting.md の「非エンジニアに内部用語を出さない」と両立させると、「開示」節（R-6(c) の解釈、未反映 17 件、監査残指摘、却下 4 件）は佐藤さんには意味が取れない。**最終報告に「人間向けの本文」と「監査向けの開示」の分離が無い**。素直な解釈: 開示は末尾にまとめて平たく書いた
[MINOR] F-37 05「完了」— 改善提案は reporting.md の「2〜5 件」に従って 5 件に絞ったが、trail の pending proposal は 13 件＋セキュリティ由来の提案。「残りは『その他の候補』として 1 行」にしたのは私の裁量。proposal の回収規則（全部列挙か要約か）が無い
[MINOR] F-38 05「完了」「最初にあなたがすること」— Python の導入手順（前提・制約で「完了報告で案内」と約束した）を最終報告に書く規定はあるが、**設置場所（stock.py をどこに置くか）の案内**は規定に無い。CLI の成果物は「起動方法」だけでは足りず、配置が要る。今回は独断で手順 2 に書いた
[HUMAN] 最終報告は長い（約 45 行）が、非エンジニアの佐藤さんに要るのは「最初にすること」「使い方」「既知の制限」の 3 節。検証結果・開示は読まれない。ただし「使ってみて困る点（未対応）: ありません」は安心材料になる

## 11. 2周目の入口（done → イテレーション → 差分ヒアリングの質問まで）

[RESUME] done 中の新要望: SKILL.md Step 4「イテレーション」どおり、interrupt 記録 → 承認リセット（イテレーション。旧承認記録を退避）→ requirements.md 承認記録を未承認に → Step 6（done → hearing、後退報告 ⏪ 理由「次の改良」）→ 対応記録「イテレーション開始」→ 同一ターンで 01 を Read し差分ヒアリングの質問を hearing-notes の未確認事項に記録して送信。作業の消失・二重実行なし。**2 周目はここ（返信待ち）で終了**（要件〜再検証は実施していない）
[MINOR] F-39 01-hearing.md「イテレーション時（差分ヒアリング）」— 今回の要望は初回ヒアリングで「不採用（今回は入れない）」とした提案2 と同じ内容で、非ゴールにも載っている。差分ヒアリングの規定は「取り下げられた要望は非ゴールへ移す」しか無く、**逆方向（非ゴールにあったものを要望として復活させる）の手順が無い**。素直な解釈: 提案2 の採否欄を「差分ヒアリング中」に書き換え、非ゴールからの取り下げをドラフトの推測に含めて確認する。02 の漏れチェックは「非ゴール節に載っている事項は漏れとみなさない」ので、非ゴールから外し忘れると要件化されない
[MINOR] F-40 01「イテレーション時」— 「変更が小さければ 1 往復で終えてよい」とあるが、承認リセット（イテレーション）の後に 02 で要件 3 周のレビューが再び走ることは変わらない（F-23 と同じコスト）。小さな追加要望 1 件のイテレーションに、承認リセット → 3 周 → ゲート → 差分仕様・設計 → 実装 → 4 役の再検証 が必ず付く。1 周目の UX 追加で実測した約 60 分がそのまま再現される見込み
[ASK] 差分ヒアリングの質問 3 問は選択式にできる（Q1: 共通 / 品目ごと、Q2: 印 / 別コマンド / 両方、Q3: はい / いいえ）。reporting.md は「選択肢の形にできるヒアリング質問」は AskUserQuestion と言うが、環境で使えないためテキスト。人間役としては、推測ドラフトに「訂正が無ければこの前提で」と書いてあるので「おまかせ」で済むのは楽
[HUMAN] done の直後に要望を送ると ⏪ が出て「追加分のヒアリングをします」と続くが、「これまでの実装は引き継ぎます」があるので不安は無い。質問 3 問は読める長さ。ただし Q3 の「初回に『今回は入れない』とした自動表示に当たるため確認」は、佐藤さんの立場では「なぜ聞かれるのか」が分かりにくい

---

## 総括

- 到達点: **done 到達（04:24）**。2周目は「done → イテレーション → 差分ヒアリングの質問送信（返信待ち）」の入口まで
- 所要: 再開 01:45 → done 04:24（2 時間 39 分）→ 2周目入口 04:27
- 欠陥件数: **CRITICAL 0 / MAJOR 9 / MINOR 24 / NIT 7（合計 40）**
  - MAJOR: F-10（仕様書の肥大化）, F-17（インライン渡しでエスケープが変換され誤指摘）, F-23（差分更新でも全文レビューで承認済み本文への指摘が出続ける）, F-24（UX「必ず直すべき」のスコープ判定基準が許容形の要件で決まらない）, F-25（対応記録の逐語引用ミスが最終監査まで検出されない）, F-30（NR 1 行の追加が仕様 152 行に膨らむ）, F-32（監査 2 周目が trail の書き方への指摘で埋まる）, F-33（開示固定の MINOR を再承認後の開発でも直せない）, F-36（最終報告が人間向けと監査向けを分離していない）
- 再開の観察: **正しく効いた**。Step 2〜5 の判定はすべて規定どおりで、3周目「着手のみ」を「やり直し」と判定し、requirements.md の再生成・二重実行・作業の消失なし。承認 2 回・承認リセット 2 回（要件変更・イテレーション）・バックログ整合・done → hearing も規定どおり
- 実測値: サブエージェント 25 体（約 2.2M tokens）、人間への質問・報告でターンを終えたもの 3 回（ゲート 2・最終報告 1）＋返信不要の報告 10 回、人間の入力 3 回。docs/ 1,344 行（spec 529 行が最大）。テスト 94 件・git commit 31
- 人間役の摩擦で一番きつかった点: **ゲート本文の「開示」**。2 回目のゲートで「承認済みの部分への 17 件は未反映」と言われ、非エンジニアには「それで大丈夫なのか」が判断できない。次いで、承認後 done まで 1 時間以上無音（同一ターン内）で、UX 由来の後退報告 ⏪ が出たあと再ゲートまで 20 分待たされる
